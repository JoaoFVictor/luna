# Replace these with the output of `id -u` and `id -g` on the host.
HOST_UID=1000
HOST_GID=1000

# Generate once per checkout with: pwd -P | sha256sum | cut -c1-24
LUNA_STUDIO_CHECKOUT_ID=replace-with-checkout-specific-id

LUNA_AUTH_ROOT=./.luna/auth
LUNA_REPOSITORIES_ROOT=./repositories
