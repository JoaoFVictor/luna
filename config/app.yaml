workspace:
  # Current workspace strategy. Only git_worktree is supported.
  strategy: git_worktree
  # Directory where Luna creates per-run worktrees.
  root: .runs/workspaces
  # Required safety assertion: terminalization never deletes successful worktrees.
  preserve_on_success: true
  # Required safety assertion: terminalization never deletes failed worktrees.
  preserve_on_failure: true
artifacts:
  # Directory where run artifacts are written.
  root: .runs
# Workflow runtime used by Luna.
workflow_runtime:
  id: langgraph
  options: {}
# Agent runtime used by Luna.
agent_runtime:
  id: pi
  options:
    # Deep reasoning/tool-call agents can legitimately take several minutes.
    request_timeout_ms: 600000
# Optional lock tuning. If omitted, Luna uses runtime defaults.
# locks:
#   root: .runs/locks
#   timeout_ms: 300000
